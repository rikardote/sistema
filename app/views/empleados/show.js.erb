// Add the dialog title
$('#dialog h3').html("<i class='glyphicon glyphicon-user'></i> Empleado");
 
// Render the new form
$('.modal-body').html('<%= j render("empleado_info") %>');
 
// Show the dynamic dialog
$('#dialog').modal("show");
 
// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
    $('.first_input').focus()
  })